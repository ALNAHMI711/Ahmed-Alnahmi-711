# سياسة الأمان
لا تضع مفاتيح Binance أو كلمات المرور أو رموز Telegram في Git. أبلغ عن الثغرات بشكل خاص لمالك المستودع. التداول الحقيقي محظور افتراضيًا، وتُحظر مفاتيح السحب ومفاتيح Binance بلا تقييد IP. تُشفّر الأسرار في قاعدة البيانات بمفتاح خارج المستودع، ولا تُدرج في النسخ الاحتياطية غير المشفرة.

## ضوابط HTTP وWebSocket
تستخدم طلبات التغيير cookie CSRF مع header `X-CSRF-Token`، وتوجد حدود طلبات وCSP ورفض uploads غير الآمنة. WebSocket لا يقبل اتصالًا بلا session صالحة، ويعيد التحقق عند الأوامر والنشر والمهلات. لا تُمنح الأحداث المحاسبية لغير admin دون ownership mapping صريح. هذه ضوابط تطبيقية أولية وليست بديلًا عن WAF، secret manager، MFA، ومراجعة مستقلة قبل LIVE.
